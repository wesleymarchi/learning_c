/* Hello world! */

#include <stdio.h>  // Biblioteca padrão C, comumente chamado de "header file" ou "header" (cabeçalho)

int main() {    // Função main
    printf("Hello World!\n");    // printf: exibe uma mensagem na tela; \n (backslash) caractere de escape

    return 0;   // Retorna 0 (zero), indica sucesso na execução
}
/* Repetição controlada por contador */

#include <stdio.h>

int main() {
  int k;

  for (k = 1; k <= 10; k++) // Loop for: inicializa variável de controle; expressão de controle; incremento/decremento
    printf("%d\n", k);

  return 0;
}
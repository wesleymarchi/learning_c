/* Repetição controlada por contador */

#include <stdio.h>

int main() {

  for (int k = 1; k <= 5; k++) // Loop for: inicializa variável de controle; expressão de controle; incremento/decremento
   	printf("%d\n", k);

  return 0;
}